"""NulPaint — voice + agentic control for a forked Krita.

The external half of the toolchain. The in-Krita half lives in
``krita_plugin/nulpaint`` and is intentionally NOT importable from here
(it runs under Krita's embedded interpreter).
"""

__version__ = "0.0.1"
